extends layout.pug

block content
    h1 All Users
    .user-grid
        each user in users
            .user-card
                img(src=`${user.photo}`, alt=`${user.name}'s photo`)
                h4 #{user.name}
                p Email: #{user.email}
                p Role: #{user.role}